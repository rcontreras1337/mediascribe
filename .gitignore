.DS_Store
Thumbs.db
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Cargo (also covered in src-tauri/.gitignore but be defensive)
target/

# IDE
.idea/
*.swp
*.swo

# Logs
*.log
logs/

# OS
.AppleDouble
.LSOverride

# User-local model cache (Whisper models can be 3 GB; never commit)
models/

# Test fixtures we don't want in repo (fixtures used by tests live in src-tauri/tests/fixtures/)
*.local
