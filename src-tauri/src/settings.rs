//! Persisted app settings (TOML in `app_data_dir`) and API key in OS keystore.
//!
//! Implementation lands in Fase 2 (parsing) and Fase 6 (keystore + persistence).

#[cfg(test)]
mod tests {
    // Tests for parse_toml and round-trip go here.
}
